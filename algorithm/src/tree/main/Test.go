package main

import (
	"fmt"
	// "os"
	// "runtime/trace"
	"gostudy/algorithm/src/tree"
)

func main() {
	// node := &tree.BiThrTreeNode{}
	root := &tree.BiThrTreeNode{Data: 1}
	fmt.Print(root)
	// fmt.Println(node)
	// tree.InitQueueCnt()
}
